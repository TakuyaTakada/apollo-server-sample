overwrite: true
schema: "src/**/*.graphql"
documents: null
config:
  typesPrefix: I
generates:
  src/__generated__/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typescript-mongodb"
