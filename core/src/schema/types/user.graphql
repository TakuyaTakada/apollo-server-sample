type User {
  id: ID!
  sub: String!
}

extend type Query {
  user(id: ID!): User
  users: [User!]!
}

input passwordAuthInput {
  email: ID!
  password: String!
}

extend type Mutation {
  signUp(input: passwordAuthInput!): User!
}
